# This is a Databricks asset bundle definition for prj2.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: prj2
  uuid: 8b5f6af9-1a03-412b-ab9d-28b0b8b1bdcf

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-2836125740739785.5.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-2836125740739785.5.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/civan@readymind.ms to make sure we only have a single copy.
      root_path: /Workspace/Users/civan@readymind.ms/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: civan@readymind.ms
        level: CAN_MANAGE
